{% extends 'dicts/base.html.twig' %}

{% block top_nav_links_right %}
    <a href="{{ path('admin_dict_branch_index') }}" class="btn btn-secondary btn-sm ml-auto mb-1 mt-1">back to list</a>
{% endblock %}

{% block title %}Edit Branch{% endblock %}

{% block body %}
    <h1>Edit Branch</h1>

    <div>
        {{ form_start(form) }}
        {{ form_widget(form) }}
        <div class="form-buttons d-flex justify-content-between">
            <button type="submit" class="btn btn-outline-primary">{{ button_label|default('Update') }}</button>
            {#            {{ include('dicts/branch/_form.html.twig', {'button_label': 'Update'}) }}#}
            {{ include('dicts/branch/_delete_form.html.twig') }}
        </div>
        {{ form_end(form) }}
    </div>
{% endblock %}
