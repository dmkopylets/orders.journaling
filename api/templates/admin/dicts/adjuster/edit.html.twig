{% extends 'admin/main.html.twig' %}
{% block top_nav_links_right %}
    <a href="{{ path('admin_dict_adjuster_index') }}" class="btn btn-secondary btn-sm ml-auto mb-1 mt-1">back to list</a>
{% endblock %}
{% block title %}Edit Adjuster{% endblock %}
{% block body %}
<div class="content">
    <h1>Edit Adjuster's data</h1>
    <div>
        {{ form_start(form) }}
        {{ form_widget(form) }}
        <div class="form-buttons d-flex justify-content-between">
            <button type="submit" class="btn btn-outline-primary">{{ button_label|default('Update') }}</button>
            {{ include('admin/dicts/adjuster/_delete_form.html.twig') }}
        </div>
        {{ form_end(form) }}
    </div>
</div>
{% endblock %}
